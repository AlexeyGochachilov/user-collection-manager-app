package ru.aston.finalproject.service.sorter;

import java.util.List;

public class ListSorter<T extends Comparable<T>> {

    public List<T> sort(List<T> list) {
        // TODO: реализовать базовую сортировку тут
        // для тестирования класса создать объект класса
        // пример: ListSorter<User> listSorter = new ListSorter<>();
        // listSorter.sort(передать список нужный)
        return list;
    }
}
